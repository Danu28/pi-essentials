node_modules/
.DS_Store
*.log
.env
.env.local
coverage/
.tmp/
.pi/
.vitest/
# dist is intentionally committed for pi extension publishing (see package.json "files": ["dist"])
# Verified via `npm pack --dry-run`. Forks that build locally can uncomment:
# dist/
